version: "3.5"

services:

  app:
    image: gcr.io/bounce-code/nodes/graphql-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    networks:
      servicenet:
        aliases:
          - graphql.internal.getbouncecode.com
          - graphql.node.internal.getbouncecode.com
    # ports:
    #   - 3000:3000


networks:
  servicenet:
    name: bouncecode-servicenet